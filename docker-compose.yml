version: "3.9"
 
services:
  backend:
    build: ./backend
    container_name: node-backend
    ports:
      - "5000:5000"
    restart: always
 
  frontend:
    build: ./frontend
    container_name: react-frontend
    ports:
      - "4000:4000"
    depends_on:
      - backend
    restart: always